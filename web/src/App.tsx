import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { Link, Navigate, Route, Routes, useLocation, useNavigate, useParams, useSearchParams } from 'react-router-dom';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { AnimatePresence, motion } from 'framer-motion';
import { Activity, ArrowUpRight, BarChart3, Check, ChevronLeft, ChevronRight, Clipboard, Code2, Inbox, LayoutDashboard, LogOut, Menu, Search, Settings, ShieldCheck, Ticket as TicketIcon, Users, UserCircle, X, PieChart as PieChartIcon } from 'lucide-react';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
import { api, Ticket, TicketCategory, TicketStatus, User } from './lib/api';
import { buildTicketQuery, fallbackPlaygroundEndpoints, parseJsonBody, type PlaygroundEndpoint } from './lib/playground';

const statusLabels: Record<TicketStatus, string> = { open: 'Open', in_progress: 'In progress', closed: 'Closed' };
const categoryLabels: Record<TicketCategory, string> = { billing: 'Billing', technical: 'Technical', general: 'General' };

function App() {
  const [user, setUser] = useState<User | null>(null);
  const [booting, setBooting] = useState(true);

  useEffect(() => {
    api.me().then(setUser).catch(() => setUser(null)).finally(() => setBooting(false));
  }, []);

  if (booting) return <div className="boot-screen"><div className="brand-mark">G</div><span>Loading workspace</span></div>;
  if (!user) return <Login onAuthenticated={setUser} />;

  return <AppShell user={user} onLogout={() => setUser(null)} />;
}

function Login({ onAuthenticated }: { onAuthenticated: (user: User) => void }) {
  const [error, setError] = useState('');
  const navigate = useNavigate();
  const form = useForm<{ email: string; password: string }>({ resolver: zodResolver(z.object({ email: z.string().email('Enter a valid email'), password: z.string().min(1, 'Password is required') })) });
  const mutation = useMutation({ mutationFn: (input: { email: string; password: string }) => api.login(input.email, input.password), onSuccess: user => { onAuthenticated(user); navigate('/dashboard'); }, onError: error => setError(error.message) });

  return <main className="login-page"><div className="login-orbit orbit-one" /><div className="login-orbit orbit-two" /><section className="login-card">
    <div className="brand-lockup"><div className="brand-mark">G</div><div><strong>GoodevaDesk</strong><span>Support operations</span></div></div>
    <div className="login-copy"><p className="eyebrow">Internal workspace</p><h1>Make every ticket easier to resolve.</h1><p>One focused queue for the conversations that need a human touch.</p></div>
    <form onSubmit={form.handleSubmit(input => { setError(''); mutation.mutate(input); })} className="form-stack">
      <label>Email<input {...form.register('email')} type="email" autoComplete="email" />{form.formState.errors.email && <small className="field-error">{form.formState.errors.email.message}</small>}</label>
      <label>Password<input {...form.register('password')} type="password" autoComplete="current-password" />{form.formState.errors.password && <small className="field-error">{form.formState.errors.password.message}</small>}</label>
      {error && <div className="form-error"><X size={16} />{error}</div>}
      <button className="button button-primary" disabled={mutation.isPending}>{mutation.isPending ? 'Signing in…' : 'Enter workspace'}<ArrowUpRight size={17} /></button>
    </form>
    <p className="login-note">Use your GoodevaDesk workspace credentials.</p>
  </section></main>;
}

function AppShell({ user, onLogout }: { user: User; onLogout: () => void }) {
  const [mobileOpen, setMobileOpen] = useState(false);
  const navigate = useNavigate();
  const location = useLocation();
  const queryClient = useQueryClient();
  const logout = useMutation({ mutationFn: api.logout, onSuccess: () => { queryClient.clear(); onLogout(); navigate('/'); } });
  const navItems = [{ label: 'Dashboard', icon: LayoutDashboard, to: '/dashboard' }, { label: 'Tickets', icon: Inbox, to: '/tickets' }, { label: 'API Playground', icon: Code2, to: '/playground' }, { label: 'Profile', icon: UserCircle, to: '/profile' }];
  const adminItems = user.role === 'admin' ? [{ label: 'Team', icon: Users, to: '/team' }, { label: 'Settings', icon: Settings, to: '/settings' }] : [];
  const sidebar = <aside className="sidebar"><div className="sidebar-top"><div className="brand-lockup"><div className="brand-mark">G</div><div><strong>GoodevaDesk</strong><span>Support operations</span></div></div><button className="icon-button mobile-close" onClick={() => setMobileOpen(false)} aria-label="Close menu"><X size={19} /></button></div><div className="workspace-switch"><span className="workspace-dot" />GoodevaDesk Production<ChevronRight size={15} /></div><nav><p className="nav-label">Workspace</p>{[...navItems, ...adminItems].map(item => <Link key={item.to} to={item.to} onClick={() => setMobileOpen(false)} className={`nav-item ${location.pathname.startsWith(item.to) ? 'active' : ''}`}><item.icon size={18} />{item.label}</Link>)}</nav><div className="sidebar-bottom"><div className="side-status"><span className="status-pulse" /><div><strong>All systems ready</strong><span>API + Redis connected</span></div></div><button className="user-menu" onClick={() => logout.mutate()}><div className="avatar">{user.name.slice(0, 1)}</div><div><strong>{user.name}</strong><span>{user.role}</span></div><LogOut size={16} /></button></div></aside>;

  const pageTitle = location.pathname === '/dashboard' ? 'Good morning, ' + user.name.split(' ')[0] : location.pathname === '/playground' ? 'API Playground' : location.pathname === '/team' || location.pathname === '/users' ? 'Team' : location.pathname.startsWith('/settings') ? 'Settings' : location.pathname === '/profile' ? 'Profile' : location.pathname.startsWith('/tickets') ? 'Ticket queue' : 'GoodevaDesk';
  return <div className="app-shell"><div className="desktop-sidebar">{sidebar}</div><AnimatePresence>{mobileOpen && <motion.div className="mobile-overlay" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ x: -320 }} onClick={() => setMobileOpen(false)}><motion.div initial={{ x: -320 }} animate={{ x: 0 }} exit={{ x: -320 }} onClick={event => event.stopPropagation()}>{sidebar}</motion.div></motion.div>}</AnimatePresence><main className="main-area"><header className="topbar"><button className="icon-button menu-button" onClick={() => setMobileOpen(true)} aria-label="Open menu"><Menu size={20} /></button><div><p className="breadcrumb">GoodevaDesk / {location.pathname === '/dashboard' ? 'Workspace' : 'Tools'}</p><h2>{pageTitle}</h2></div><div className="topbar-actions"><div className="system-chip"><Activity size={15} />Live workspace</div><div className="avatar avatar-small">{user.name.slice(0, 1)}</div></div></header><div className="content-area"><Routes><Route path="/" element={<Navigate to="/dashboard" replace />} /><Route path="/dashboard" element={<Dashboard />} /><Route path="/tickets" element={<Tickets />} /><Route path="/tickets/:ticketId" element={<TicketDetail />} /><Route path="/playground" element={<Playground />} /><Route path="/profile" element={<Profile user={user} />} /><Route path="/team" element={user.role === 'admin' ? <Team /> : <Navigate to="/dashboard" replace />} /><Route path="/users" element={user.role === 'admin' ? <Team /> : <Navigate to="/dashboard" replace />} /><Route path="/settings" element={user.role === 'admin' ? <SettingsPage /> : <Navigate to="/dashboard" replace />} /><Route path="/settings/organization" element={user.role === 'admin' ? <SettingsPage /> : <Navigate to="/dashboard" replace />} /><Route path="/settings/api-key" element={user.role === 'admin' ? <SettingsPage /> : <Navigate to="/dashboard" replace />} /><Route path="*" element={<EmptyRoute />} /></Routes></div></main></div>;
}

function Dashboard() {
  const { data, isLoading, isError } = useQuery({ queryKey: ['dashboard', 'summary'], queryFn: api.summary });
  const stats = [{ label: 'Total tickets', value: data?.total ?? 0, icon: TicketIcon, tone: 'blue' }, { label: 'Needs attention', value: data?.open ?? 0, icon: Activity, tone: 'orange' }, { label: 'In progress', value: data?.in_progress ?? 0, icon: BarChart3, tone: 'purple' }, { label: 'Resolved', value: data?.closed ?? 0, icon: Check, tone: 'green' }];
  const categoryData = (data?.categories ?? []).map(item => ({ name: item.category ? categoryLabels[item.category] : 'Pending', value: item._count._all }));
  const categoryColors = ['#4f46e5', '#06b6d4', '#f59e0b', '#94a3b8'];
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Tuesday, September 24</p><h1>Your support pulse</h1><p className="page-subtitle">A calm view of what your team needs to pick up next.</p></div><Link to="/tickets" className="button button-primary"><Inbox size={17} />Open ticket queue</Link></div>{isError && <div className="notice notice-warning">The API is currently unavailable. Start the local API and refresh to load live workspace data.</div>}<div className="stat-grid">{stats.map(({ label, value, icon: Icon, tone }) => <motion.article key={label} className="stat-card" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }}><div className={`stat-icon ${tone}`}><Icon size={19} /></div><div><span>{label}</span><strong>{isLoading ? '—' : value}</strong></div><ArrowUpRight size={16} className="stat-arrow" /></motion.article>)}</div><div className="dashboard-grid"><section className="panel panel-wide"><div className="panel-heading"><div><p className="eyebrow">Queue rhythm</p><h2>Recent tickets</h2></div><Link to="/tickets" className="text-link">View all <ArrowUpRight size={15} /></Link></div>{data?.recent?.length ? <div className="ticket-list">{data.recent.map(ticket => <TicketRow key={ticket.id} ticket={ticket} />)}</div> : <EmptyState title="Your queue is clear" copy="New tickets will appear here as soon as the API receives them." />}</section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Reliability</p><h2>Workspace health</h2></div><ShieldCheck size={19} className="health-icon" /></div><div className="health-card"><span className="health-ring"><Check size={20} /></span><div><strong>Everything is ready</strong><p>API and Redis are available for ticket operations.</p></div></div><div className="health-lines"><div><span>API response time</span><strong>128 ms</strong></div><div><span>LLM enrichment</span><strong className="green-text">Protected</strong></div><div><span>Tenant boundary</span><strong className="green-text">Active</strong></div></div></section></div><section className="panel category-panel"><div className="panel-heading"><div><p className="eyebrow">Triage mix</p><h2>Tickets by category</h2></div><PieChartIcon size={18} className="health-icon" /></div>{categoryData.length ? <div className="category-chart"><ResponsiveContainer width="100%" height={220}><PieChart><Pie data={categoryData} dataKey="value" nameKey="name" innerRadius={58} outerRadius={82} paddingAngle={4}>{categoryData.map((entry, index) => <Cell key={entry.name} fill={categoryColors[index % categoryColors.length]} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer><div className="category-legend">{categoryData.map((entry, index) => <span key={entry.name}><i style={{ background: categoryColors[index % categoryColors.length] }} />{entry.name} <strong>{entry.value}</strong></span>)}</div></div> : <EmptyState title="Category data will appear here" copy="Create a ticket to populate the triage mix." />}</section></PageTransition>;
}

function Tickets() {
  const [searchParams, setSearchParams] = useSearchParams();
  const page = Number(searchParams.get('page') ?? 1);
  const search = searchParams.get('search') ?? '';
  const status = searchParams.get('status') ?? '';
  const category = searchParams.get('category') ?? '';
  const params = useMemo(() => buildTicketQuery({ page, limit: 10, search, status, category }), [page, search, status, category]);
  const query = useQuery({ queryKey: ['tickets', params.toString()], queryFn: () => api.tickets(params) });
  const update = (key: string, value: string) => { const next = new URLSearchParams(searchParams); if (value) next.set(key, value); else next.delete(key); next.set('page', '1'); setSearchParams(next); };
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Operations / Queue</p><h1>Ticket queue</h1><p className="page-subtitle">Sort through the conversations that need a thoughtful next move.</p></div><div className="queue-count"><span>Showing</span><strong>{query.data?.meta.total ?? 0}</strong></div></div><section className="panel queue-panel"><div className="filter-bar"><label className="search-field"><Search size={17} /><input value={search} onChange={event => update('search', event.target.value)} placeholder="Search subject, customer, or message" /></label><select value={status} onChange={event => update('status', event.target.value)}><option value="">All statuses</option><option value="open">Open</option><option value="in_progress">In progress</option><option value="closed">Closed</option></select><select value={category} onChange={event => update('category', event.target.value)}><option value="">All categories</option><option value="billing">Billing</option><option value="technical">Technical</option><option value="general">General</option></select></div>{query.isLoading ? <div className="skeleton-list">{[1, 2, 3, 4].map(item => <div className="skeleton-row" key={item} />)}</div> : query.isError ? <EmptyState title="We could not load the queue" copy="Check the API connection, then try again." action={<button className="button button-secondary" onClick={() => query.refetch()}>Try again</button>} /> : query.data?.data.length ? <div className="ticket-list">{query.data.data.map(ticket => <TicketRow key={ticket.id} ticket={ticket} />)}</div> : <EmptyState title={search || status || category ? 'No tickets match these filters' : 'No tickets yet'} copy={search || status || category ? 'Clear a filter to widen the queue.' : 'Tickets created through the integration API will appear here.'} action={(search || status || category) ? <button className="text-link" onClick={() => setSearchParams({ page: '1' })}>Reset filters</button> : undefined} />}</section>{query.data && query.data.meta.total_pages > 1 && <div className="pagination"><button className="icon-button" disabled={page <= 1} onClick={() => update('page', String(page - 1))} aria-label="Previous page"><ChevronLeft size={18} /></button><span>Page {page} of {query.data.meta.total_pages}</span><button className="icon-button" disabled={page >= query.data.meta.total_pages} onClick={() => update('page', String(page + 1))} aria-label="Next page"><ChevronRight size={18} /></button></div>}</PageTransition>;
}

function TicketRow({ ticket }: { ticket: Ticket }) {
  return <Link to={`/tickets/${ticket.id}`} className="ticket-row"><div className="ticket-main"><span className="ticket-avatar">{ticket.customerEmail.slice(0, 1).toUpperCase()}</span><div><strong>{ticket.subject}</strong><span>{ticket.customerEmail}</span></div></div><div className="ticket-message">{ticket.message}</div><div className="ticket-meta">{ticket.category ? <span className={`category-badge ${ticket.category}`}>{categoryLabels[ticket.category]}</span> : <span className="category-badge pending">Pending AI</span>}<span className={`status-badge ${ticket.status}`}>{statusLabels[ticket.status]}</span></div><ArrowUpRight size={17} className="row-arrow" /></Link>;
}

function TicketDetail() {
  const { ticketId } = useParams();
  const navigate = useNavigate();
  const queryClient = useQueryClient();
  const query = useQuery({ queryKey: ['ticket', ticketId], queryFn: () => api.ticket(ticketId!), enabled: Boolean(ticketId) });
  const update = useMutation({ mutationFn: (status: TicketStatus) => api.updateStatus(ticketId!, status), onSuccess: ticket => { queryClient.setQueryData(['ticket', ticketId], ticket); queryClient.invalidateQueries({ queryKey: ['tickets'] }); } });
  const [copied, setCopied] = useState(false);
  if (query.isLoading) return <PageTransition><div className="detail-loading"><div className="skeleton-detail" /><div className="skeleton-detail small" /></div></PageTransition>;
  if (query.isError || !query.data) return <PageTransition><EmptyState title="Ticket not found" copy="This ticket may have been removed or belongs to another workspace." action={<button className="button button-secondary" onClick={() => navigate('/tickets')}>Back to queue</button>} /></PageTransition>;
  const ticket = query.data;
  const copyReply = async () => { if (!ticket.suggestedReply) return; await navigator.clipboard.writeText(ticket.suggestedReply); setCopied(true); setTimeout(() => setCopied(false), 1800); };
  return <PageTransition><button className="back-link" onClick={() => navigate('/tickets')}><ChevronLeft size={16} />Back to queue</button><div className="detail-layout"><section className="panel detail-main"><div className="detail-heading"><div><p className="eyebrow">Ticket detail</p><h1>{ticket.subject}</h1><p className="detail-customer">{ticket.customerEmail} · {new Date(ticket.createdAt).toLocaleString()}</p></div><span className={`status-badge large ${ticket.status}`}>{statusLabels[ticket.status]}</span></div><div className="message-card"><p className="eyebrow">Customer message</p><p>{ticket.message}</p></div><div className="reply-section"><div className="panel-heading"><div><p className="eyebrow">AI assist</p><h2>Suggested reply</h2></div>{ticket.category && <span className={`category-badge ${ticket.category}`}>{categoryLabels[ticket.category]}</span>}</div>{ticket.suggestedReply ? <div className="reply-card"><p>{ticket.suggestedReply}</p><button className="button button-secondary" onClick={copyReply}>{copied ? <Check size={16} /> : <Clipboard size={16} />}{copied ? 'Copied' : 'Copy reply'}</button></div> : <div className="unavailable-card"><Activity size={18} /><div><strong>Suggestion unavailable</strong><p>The ticket is saved safely. AI enrichment can be retried later.</p></div></div>}</div></section><aside className="panel detail-side"><p className="eyebrow">Workflow</p><h2>Move this ticket forward</h2><p className="side-copy">Status changes are visible to the whole organization workspace.</p><div className="status-actions">{(['open', 'in_progress', 'closed'] as TicketStatus[]).map(status => <button key={status} className={`status-action ${ticket.status === status ? 'selected' : ''}`} onClick={() => update.mutate(status)} disabled={update.isPending}><span className={`status-dot ${status}`} />{statusLabels[status]}{ticket.status === status && <Check size={16} />}</button>)}</div>{update.isError && <div className="form-error">Could not update status.</div>}</aside></div></PageTransition>;
}

function Team() {
  const query = useQuery({ queryKey: ['team'], queryFn: api.users });
  const form = { name: '', email: '', password: '', role: 'agent' as 'agent' | 'admin' };
  const [notice, setNotice] = useState('');
  const create = useMutation({ mutationFn: (input: typeof form) => api.createUser(input), onSuccess: () => { formApi.reset(); setNotice('Member added successfully.'); query.refetch(); }, onError: error => setNotice(error.message) });
  const remove = useMutation({ mutationFn: api.deleteUser, onSuccess: () => query.refetch(), onError: error => setNotice(error.message) });
  const updateRole = useMutation({ mutationFn: ({ id, role }: { id: string; role: 'agent' | 'admin' }) => api.updateUser(id, { role }), onSuccess: () => query.refetch(), onError: error => setNotice(error.message) });
  const teamSchema = z.object({ name: z.string().min(2), email: z.string().email(), password: z.string().min(12), role: z.enum(['agent', 'admin']) });
  const formApi = useForm<typeof form>({ resolver: zodResolver(teamSchema), defaultValues: form });
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Workspace / People</p><h1>Your team</h1><p className="page-subtitle">Keep access aligned with the people resolving customer conversations.</p></div><span className="safe-chip"><ShieldCheck size={15} />Admin only</span></div><div className="team-grid"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Members</p><h2>{query.data?.length ?? 0} people</h2></div><Users size={18} className="health-icon" /></div><div className="member-list">{query.isLoading ? <div className="skeleton-row" /> : query.data?.map(member => <div className="member-row" key={member.id}><div className="avatar">{member.name.slice(0, 1)}</div><div className="member-info"><strong>{member.name}</strong><span>{member.email}</span></div><select className="role-select" value={member.role} onChange={event => updateRole.mutate({ id: member.id, role: event.target.value as 'agent' | 'admin' })}><option value="agent">Agent</option><option value="admin">Admin</option></select><button className="icon-button danger-button" aria-label={`Delete ${member.name}`} onClick={() => { if (window.confirm(`Delete ${member.name}?`)) remove.mutate(member.id); }}><X size={15} /></button></div>)}</div></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Invite member</p><h2>Add access</h2></div></div><form className="form-stack" onSubmit={formApi.handleSubmit(input => { setNotice(''); create.mutate(input); })}><label>Name<input {...formApi.register('name')} placeholder="Alex Morgan" />{formApi.formState.errors.name && <small className="field-error">Name is required.</small>}</label><label>Email<input {...formApi.register('email')} type="email" placeholder="alex@example.com" /></label><label>Temporary password<input {...formApi.register('password')} minLength={12} type="password" placeholder="At least 12 characters" /></label><label>Role<select {...formApi.register('role')}><option value="agent">Agent</option><option value="admin">Admin</option></select></label>{notice && <div className="notice notice-warning">{notice}</div>}<button className="button button-primary" disabled={create.isPending}>{create.isPending ? 'Adding…' : 'Add member'}<ArrowUpRight size={16} /></button></form></section></div></PageTransition>;
}

function SettingsPage() {
  const health = useQuery({ queryKey: ['health'], queryFn: async () => { const response = await fetch('/api/health'); if (!response.ok) throw new Error('Health check failed'); return response.json() as Promise<{ status: string; dependencies: { database: boolean; redis: boolean } }>; } });
  const organization = useQuery({ queryKey: ['organization'], queryFn: api.organization });
  const [name, setName] = useState('');
  const [rotatedKey, setRotatedKey] = useState('');
  useEffect(() => { if (organization.data) setName(organization.data.name); }, [organization.data]);
  const save = useMutation({ mutationFn: () => api.updateOrganization(name), onSuccess: () => organization.refetch() });
  const rotate = useMutation({ mutationFn: api.rotateApiKey, onSuccess: data => setRotatedKey(data.apiKey) });
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Workspace / Operations</p><h1>Settings</h1><p className="page-subtitle">Review workspace identity, integration access, and production dependencies.</p></div><span className="safe-chip"><Activity size={15} />Live checks</span></div><div className="settings-grid"><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Service health</p><h2>Dependencies</h2></div><ShieldCheck size={18} className="health-icon" /></div><div className="settings-lines"><div><span>API status</span><strong className="green-text">{health.isLoading ? 'Checking…' : health.data?.status === 'ok' ? 'Operational' : 'Degraded'}</strong></div><div><span>PostgreSQL</span><strong className={health.data?.dependencies.database ? 'green-text' : ''}>{health.isLoading ? 'Checking…' : health.data?.dependencies.database ? 'Connected' : 'Unavailable'}</strong></div><div><span>Redis cache</span><strong className={health.data?.dependencies.redis ? 'green-text' : ''}>{health.isLoading ? 'Checking…' : health.data?.dependencies.redis ? 'Connected' : 'Unavailable'}</strong></div></div></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Organization</p><h2>Workspace identity</h2></div><Settings size={18} className="health-icon" /></div><form className="form-stack" onSubmit={event => { event.preventDefault(); save.mutate(); }}><label>Organization name<input value={name} onChange={event => setName(event.target.value)} /></label><button className="button button-secondary" disabled={save.isPending}>{save.isPending ? 'Saving…' : 'Save organization'}</button></form></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">API key</p><h2>Integration access</h2></div><Code2 size={18} className="health-icon" /></div><p className="side-copy">Current key: <code>{organization.data?.apiKey ?? 'Loading…'}</code></p><button className="button button-secondary" onClick={() => { if (window.confirm('Rotate the organization API key?')) rotate.mutate(); }} disabled={rotate.isPending}>{rotate.isPending ? 'Rotating…' : 'Rotate API key'}</button>{rotatedKey && <div className="notice notice-warning">New key (shown once): <code>{rotatedKey}</code></div>}<p className="side-copy">Use the API Playground to test tenant-scoped requests. Keys stay in memory in the browser.</p><Link to="/playground" className="button button-secondary">Open API Playground<ArrowUpRight size={16} /></Link></section></div></PageTransition>;
}

function Profile({ user }: { user: User }) {
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Account</p><h1>Your profile</h1><p className="page-subtitle">Your current workspace identity and access level.</p></div><span className="safe-chip"><UserCircle size={15} />Signed in</span></div><section className="panel profile-card"><div className="avatar profile-avatar">{user.name.slice(0, 1)}</div><div><p className="eyebrow">Workspace member</p><h2>{user.name}</h2><p className="side-copy">{user.email}</p><span className="role-badge">{user.role}</span></div></section></PageTransition>;
}

function Playground() {
  const [endpoints, setEndpoints] = useState<PlaygroundEndpoint[]>(fallbackPlaygroundEndpoints);
  const [selectedId, setSelectedId] = useState(fallbackPlaygroundEndpoints[0].id);
  const [apiKey, setApiKey] = useState('');
  const [body, setBody] = useState(fallbackPlaygroundEndpoints[0].body);
  const [pathValue, setPathValue] = useState('');
  const [queryString, setQueryString] = useState('');
  const [requestError, setRequestError] = useState('');
  const [response, setResponse] = useState<{ status: number; duration: number; body: string } | null>(null);
  const [pending, setPending] = useState(false);
  useEffect(() => { fetch('/api/openapi.json').then(result => result.ok ? result.json() : Promise.reject()).then(spec => { const fromSpec = Object.entries((spec as { paths?: Record<string, Record<string, { summary?: string; description?: string }> > }).paths ?? {}).flatMap(([path, methods]) => Object.entries(methods).map(([method, operation]) => { const fallback = fallbackPlaygroundEndpoints.find(item => item.path.replace(':id', '{id}') === path && item.method === method.toUpperCase()); return fallback ? { ...fallback, description: operation.description ?? operation.summary ?? fallback.description } : null; }).filter((item): item is PlaygroundEndpoint => Boolean(item))); if (fromSpec.length) setEndpoints(fromSpec); }).catch(() => undefined); }, []);
  const endpoint = endpoints.find(item => item.id === selectedId) ?? endpoints[0];
  const chooseEndpoint = (id: string) => { const next = endpoints.find(item => item.id === id) ?? endpoints[0]; setSelectedId(id); setBody(next.body); setResponse(null); setRequestError(''); };
  const send = async () => {
    setRequestError('');
    if (endpoint.path.includes(':id') && !pathValue.trim()) { setRequestError('Ticket ID is required.'); return; }
    const parsed = endpoint.method === 'GET' ? { ok: true as const, value: undefined } : parseJsonBody(body);
    if (!parsed.ok) { setRequestError(parsed.error); return; }
    setPending(true);
    const path = endpoint.path.replace(':id', encodeURIComponent(pathValue)) + (endpoint.method === 'GET' && queryString.trim() ? `?${queryString.replace(/^\?/, '')}` : '');
    const started = performance.now();
    try {
      const result = await fetch(`/api${path}`, { method: endpoint.method, credentials: 'include', headers: { 'Content-Type': 'application/json', ...(apiKey ? { 'x-api-key': apiKey } : {}) }, body: endpoint.method === 'GET' ? undefined : JSON.stringify(parsed.value) });
      const text = await result.text();
      setResponse({ status: result.status, duration: Math.round(performance.now() - started), body: text ? JSON.stringify(JSON.parse(text), null, 2) : '' });
    } catch (error) {
      setResponse({ status: 0, duration: Math.round(performance.now() - started), body: error instanceof Error ? error.message : 'Network error' });
    } finally { setPending(false); }
  };
  return <PageTransition><div className="page-intro"><div><p className="eyebrow">Developer tools</p><h1>API Playground</h1><p className="page-subtitle">Try the documented integration API without leaving the workspace.</p></div><div className="safe-chip"><ShieldCheck size={15} />Same-origin only</div></div><div className="playground-grid"><section className="panel endpoint-panel"><div className="panel-heading"><div><p className="eyebrow">OpenAPI surface</p><h2>Endpoints</h2></div><Code2 size={18} className="health-icon" /></div><div className="endpoint-list">{endpoints.map(item => <button key={item.id} className={`endpoint-item ${selectedId === item.id ? 'selected' : ''}`} onClick={() => chooseEndpoint(item.id)}><span className={`method-pill ${item.method.toLowerCase()}`}>{item.method}</span><span><strong>{item.path}</strong><small>{item.description}</small></span></button>)}</div><a className="text-link docs-link" href="/api/docs" target="_blank" rel="noreferrer">Open Swagger docs <ArrowUpRight size={15} /></a></section><section className="panel request-panel"><div className="request-header"><div><p className="eyebrow">Request builder</p><h2><span className={`method-pill ${endpoint.method.toLowerCase()}`}>{endpoint.method}</span> {endpoint.path}</h2></div><button className="button button-primary" onClick={send} disabled={pending}>{pending ? 'Sending…' : 'Send request'}<ArrowUpRight size={16} /></button></div><p className="request-description">{endpoint.description}</p><label className="field-label">Organization API key <span>kept in memory only</span><input type="password" value={apiKey} onChange={event => setApiKey(event.target.value)} placeholder="x-api-key" /></label>{endpoint.path === '/tickets' && <label className="field-label">Query string <span>example: page=1&amp;limit=10&amp;status=open</span><input value={queryString} onChange={event => setQueryString(event.target.value)} placeholder="page=1&limit=10" /></label>}{endpoint.path.includes(':id') && <label className="field-label">Ticket ID<input value={pathValue} onChange={event => setPathValue(event.target.value)} placeholder="UUID" /></label>}{endpoint.method !== 'GET' && <label className="field-label">JSON body<textarea value={body} onChange={event => setBody(event.target.value)} spellCheck={false} /></label>}{requestError && <div className="form-error"><X size={15} />{requestError}</div>}<div className="generated-example"><div><span className="eyebrow">Generated cURL</span><button className="copy-example" onClick={() => navigator.clipboard.writeText(`curl -X ${endpoint.method} https://goodevadesk.fatihur.web.id/api${endpoint.path} -H 'x-api-key: $GOODEVA_API_KEY'`)}><Clipboard size={14} />Copy</button></div><code>curl -X {endpoint.method} https://goodevadesk.fatihur.web.id/api{endpoint.path} -H 'x-api-key: $GOODEVA_API_KEY'</code></div></section><section className="panel response-panel"><div className="panel-heading"><div><p className="eyebrow">Response viewer</p><h2>Result</h2></div>{response && <span className={`response-status ${response.status >= 200 && response.status < 300 ? 'success' : 'error'}`}>{response.status} · {response.duration} ms</span>}</div>{response ? <pre className="response-body">{response.body}</pre> : <EmptyState title="No request yet" copy="Choose an endpoint, review the request, then send it to see the response." />}</section></div></PageTransition>;
}

function PageTransition({ children }: { children: ReactNode }) { return <motion.div className="page-transition" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.22 }}>{children}</motion.div>; }
function EmptyState({ title, copy, action }: { title: string; copy: string; action?: ReactNode }) { return <div className="empty-state"><div className="empty-icon"><Inbox size={21} /></div><h3>{title}</h3><p>{copy}</p>{action}</div>; }
function EmptyRoute() { return <EmptyState title="This page is still taking shape" copy="Use the dashboard or ticket queue to continue working." action={<Link to="/dashboard" className="button button-secondary">Go to dashboard</Link>} />; }

export default App;
